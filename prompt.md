Create a typescript website which loads a list of images with a json file (it requests the json file and then loads it). It contains an array of images and these images each are a jpg file and also have a json file associated with it (same name just extension is .json) and then these get loaded. 

The UI:
yu have two tabs at the top centered one Control and one build tab when clicking the control tab it shows a iframe of a new folder /control/index.html which is an extra page and when clicking build it shows a plus in the middle where you can click on and then have a popup where all the images are and in these images are all the windows colored in (the windows are in the json file it contains a name and a list of all windows (key: windows) and each window has a x, y, w, h key). these images can then be selected and are added to a list and shown to the user next to each other so you can build your own background and then you can also click on the windows and when clicking on the window it either shows that it is on (yellow) or it is off (black) (overlays). and on the top right orner is a gear where you can change settings that get saved in the localStorage (later might be saved on a server (but not yet)). and in the settings you can choose two main lighting: day-night-cylce:
there you can also check party at night, tv at night, or similar things on the day (add some that could be cool) and there is also a custom-color tab and there you can set a custom color (use a good locking color picker) and there is also a tab where you can add "nodes" to a list and these nodes can be run after one another one is just a delay and you can choose a fixed delay or a random delay between x and y; you can change the color (also randomizable) and you can have transitions from one coor to another in the time of x; you can also have a flicker node that flickers x times a second(s) (you can set on off time)

and the website should be in german